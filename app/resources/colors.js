/**
 * Colors - global config for colors.
 * This file deliberately breaks the components own their own stuff rule
 * to ensure consistant colors throughout the app.
 */

const colors = {
  white: '#FAFAFA',
  pink: '#fde0e0',
  blue: 'blue',
};

export default colors;
